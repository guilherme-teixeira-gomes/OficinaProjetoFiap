apiVersion: v1
kind: Service
metadata:
  name: oficina-svc
  labels:
    app: oficina-api
spec:
  type: LoadBalancer
  selector:
    app: oficina-api
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
